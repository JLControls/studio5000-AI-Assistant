# Core web parsing dependencies
beautifulsoup4>=4.12.0
lxml>=4.9.0

# Data handling and async support
asyncio>=3.4.3

# Dependency-graph engine for iterative PLC comment analysis (comment_graph)
networkx>=3.0

# XML processing (for L5X generation)
xml-utils>=1.0.0

# Vendored patched ACD parser/exporter runtime
kaitaistruct>=0.11
loguru>=0.7.3

# Test tooling
pytest>=8.0.0

# Vector database and embeddings for SDK documentation search (FIXED VERSIONS)
torch==2.2.2
sentence-transformers==2.7.0
faiss-cpu>=1.7.0
numpy<2.0.0

# PDF parsing and analysis for technical drawings
PyMuPDF>=1.26.0
pdfplumber>=0.11.0
PyPDF2>=3.0.0

# Studio 5000 SDK integration is intentionally disabled. ACD analysis uses the
# vendored parser/exporter and targets Studio 5000 v38 L5X semantics.
