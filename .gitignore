# Python
__pycache__/
*.pyc
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# Virtual environments
.env
.venv
env/
venv/
ENV/
env.bak/
venv.bak/

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# Local MCP client config (machine-specific absolute paths / doc + SDK roots)
.mcp.json

# OS
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Vector Database Cache Files (Generated Locally)
instruction_vector_cache/
sdk_vector_cache/
test_sdk_cache/
*_vector_cache/
*.pkl
*.faiss

# Test Files and Generated Projects
*.ACD
*.L5X
# Hand-authored, project-agnostic Ignition exporter test fixture (no proprietary
# data) — must be tracked so the committed test suite can run in a clean checkout.
!tests/ignition_exporter/fixtures/synthetic_ignition.L5X
# Hand-authored, project-agnostic multi-program fixture for the structural project
# overview tests (issue #9) — no proprietary data; tracked for clean-checkout runs.
!tests/l5x_analyzer/fixtures/multiprogram.L5X
# Hand-authored, project-agnostic fixture for the tag/AOI fact-accessor tests
# (issue #28) — no proprietary data; tracked for clean-checkout runs.
!tests/l5x_analyzer/fixtures/fact_accessor.L5X
# Scratch scripts at the repo root only — do NOT ignore the pytest suite under tests/.
/test_*.py
/debug_*.py
/*_test_*

# Studio 5000 specific
*.L5K
*.RSS

# Log files
*.log
logs/

# Debug output directories
acd-extract-debug/

tests/acd/KemcoWaterHeater/ignitionTagsExisting.json
tests/acd/KemcoWaterHeater/Kemco_HA105-Tags.CSV
kemco_out/all.json
kemco_out/curated.json
.worktrees/
